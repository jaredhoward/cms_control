<h2>Content Information</h2>
<%= f.hidden_field :id unless f.object.new_record? %>
<div class="input">
  <%= f.label :title, 'Content Title', :class => 'title' %>
  <div class="field">
    <%= f.text_field :title %>
  </div>
</div>
<div class="input">
  <%= f.label :content, 'Content', :class => 'title' %>
  <div class="field mce-editor">
    <%= f.text_area :content, :class => 'mceEditor' %>
  </div>
</div>
