<td><%= cms_meta.access %></td>
<td>
  <div><%=h cms_meta.url %></div>
  <div class="actions">
    <%= link_to 'Show', url_for_meta(cms_meta), :popup => true, :class => 'last', :title => 'View in New Window' %>
  </div>
</td>